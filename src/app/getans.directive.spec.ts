import { GetansDirective } from './getans.directive';

describe('GetansDirective', () => {
  it('should create an instance', () => {
    const directive = new GetansDirective();
    expect(directive).toBeTruthy();
  });
});
